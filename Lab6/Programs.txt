//1st program

library(ggplot2)
dotchart(mtcars$mpg, labels = row.names(mtcars),cex = 0.6, xlab = "mpg")


//2nd program

library("gcookbook")
df = cabbage_exp
head(df)
barplot(Weight ~ Cultivar+Date, data=df, xlab = "Date", ylab = "weight", beside=TRUE, legend = TRUE, col =c("orange3","blue1"))
